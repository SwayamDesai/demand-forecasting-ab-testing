# Reproducibility manifest. Per our plan we install LAZILY (per phase) to keep
# early setup light; this file lists everything so a clean-room `make install`
# can rebuild the full environment. Versions get frozen at v1 (pip freeze).

# ---- Phase 0/1: data + EDA (installed now) ----
pandas>=2.2
numpy>=1.26
kaggle>=1.6
matplotlib>=3.8
seaborn>=0.13
jupyter>=1.0

# ---- Phase 2: classical + ML baselines (install before Phase 2) ----
statsforecast>=1.7
statsmodels>=0.14
prophet>=1.1
lightgbm>=4.3
scikit-learn>=1.4

# ---- Phase 3: deep-learning challenger (install before Phase 3) ----
torch>=2.2
# pytorch-forecasting>=1.0   # optional TFT (the "modern" angle); enable in Phase 3

# ---- Phase 4: experiment / statistics ----
scipy>=1.12

# ---- experiment tracking (MLflow) ----
mlflow>=2.12

# ---- testing ----
pytest>=8.0
