# raw + processed data (downloaded via `make data`, never committed)
data/

# python virtualenv + caches
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# experiment tracking artifacts
mlruns/
mlartifacts/
mlflow.db
mlflow.db-journal

# os cruft
.DS_Store

# NOTE: reports/ PNG plots + small summary CSVs are intentionally tracked so they
# render on GitHub without anyone running the pipeline. But the LARGE, fully
# regenerable derived tables are not — regenerate them with the phase scripts.
reports/phase5_dashboards/forecasts_long.csv
reports/phase5_dashboards/ab_readout.csv
reports/phase4_experiment/experiment_long_table.csv
reports/phase4_experiment_v2/experiment_long_table.csv
